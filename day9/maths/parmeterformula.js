const pararec = (l,b) => 2( l * b);
const parsqu = (s) => 4*s;
const parcir = (r) => 2 * 3.14 * r * r ;

const parObj ={
    pararec: pararec,
    parsqu: parsqu,
    parcir:parcir
 };

 module.exports = parObj;
